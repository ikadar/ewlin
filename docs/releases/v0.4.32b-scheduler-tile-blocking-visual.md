# Release v0.4.32b – Scheduler Tile Blocking Visual & Tooltip

> **Status:** 🔴 Not Started
>
> **Milestone:** M4
>
> **Target Date:** TBD
>
> **Git Tag:** `v0.4.32b`

---

## Overview

Add visual feedback on scheduler tiles to indicate when an element is blocked due to missing prerequisites. Blocked tiles show a dashed left border, and hovering for 2 seconds displays a tooltip with prerequisite status details.

---

## Prerequisites

- [x] `v0.4.32a` released (Element Prerequisites Data Model)

---

## Scope

### In Scope

- Blocking logic implementation
- Dashed left border for blocked tiles
- Custom tooltip on 2-second hover (blocked tiles only)
- Tooltip shows status of each prerequisite with ⚠/✓ indicators

### Out of Scope

- Forme status (v0.4.32c)
- Date tracking (v0.4.32c)
- Backend API changes

---

## Design

### Blocking Logic

An element is "blocked" if any prerequisite is not in a ready state:

```typescript
function isElementBlocked(element: Element): boolean {
  const paperReady = ['none', 'in_stock', 'delivered'].includes(element.paperStatus);
  const batReady = ['none', 'bat_approved'].includes(element.batStatus);
  const platesReady = ['none', 'ready'].includes(element.plateStatus);

  return !paperReady || !batReady || !platesReady;
}
```

### Tile Visual Changes

| State | Left Border Style |
|-------|-------------------|
| Ready (not blocked) | Solid 4px job color |
| Blocked | Dashed 4px job color |

CSS for dashed border:
```css
border-left: 4px dashed var(--job-color);
```

### Tooltip Design

Tooltip appears after **2 seconds** of hover on a **blocked** tile only.

**Content format:**
```
⚠ Papier: Commandé
⚠ BAT: Attente fichiers
✓ Plaques: Prêtes
```

- ⚠ (warning) for blocking items (not ready)
- ✓ (check) for ready items
- Only show relevant prerequisites (hide Plates if `none`)

### Tooltip Styling

- Dark background (`bg-slate-800`)
- White text
- Rounded corners
- Shadow
- Positioned above tile, centered
- Arrow pointing down

---

## Feature Checklist

### Blocking Logic

- [ ] **Create isElementBlocked utility**
  - Description: Function to determine if element is blocked
  - Files: `apps/web/src/utils/prerequisites.ts`

- [ ] **Export blocking status from element data**
  - Description: Add isBlocked computed property or utility usage
  - Files: `apps/web/src/hooks/useSchedulerData.ts` or similar

### Tile Visual

- [ ] **Add blocked prop to Tile component**
  - Description: Accept blocked boolean to change border style
  - Files: `apps/web/src/components/Tile/Tile.tsx`

- [ ] **Implement dashed border for blocked tiles**
  - Description: CSS conditional for dashed vs solid border
  - Files: `apps/web/src/components/Tile/Tile.tsx`

- [ ] **Pass blocked status to tiles**
  - Description: Calculate and pass isBlocked to each tile
  - Files: `apps/web/src/components/StationColumns/StationColumn.tsx`

### Tooltip

- [ ] **Create PrerequisiteTooltip component**
  - Description: Custom tooltip showing prerequisite status
  - Files: `apps/web/src/components/Tile/PrerequisiteTooltip.tsx`

- [ ] **Implement 2-second hover delay**
  - Description: Show tooltip only after 2s of continuous hover
  - Files: `apps/web/src/components/Tile/Tile.tsx`

- [ ] **Format tooltip content**
  - Description: ⚠/✓ indicators with French labels
  - Files: `apps/web/src/components/Tile/PrerequisiteTooltip.tsx`

- [ ] **Tooltip positioning**
  - Description: Position above tile, centered, with arrow
  - Files: `apps/web/src/components/Tile/PrerequisiteTooltip.tsx`

---

## Testing Requirements

### Unit Tests

- [ ] `isElementBlocked` returns true when paper not ready
- [ ] `isElementBlocked` returns true when BAT not ready
- [ ] `isElementBlocked` returns true when plates not ready
- [ ] `isElementBlocked` returns false when all prerequisites ready
- [ ] `isElementBlocked` treats 'none' status as ready

### E2E Tests (Playwright)

- [ ] Blocked tile shows dashed border
- [ ] Ready tile shows solid border
- [ ] Hover 2s on blocked tile → tooltip appears
- [ ] Hover <2s on blocked tile → no tooltip
- [ ] Hover on ready tile → no tooltip (regardless of duration)
- [ ] Tooltip shows correct ⚠/✓ indicators

---

## Manual QA Test Plan

### Visual Verification
- [ ] Blocked tiles have dashed left border
- [ ] Ready tiles have solid left border
- [ ] Tooltip appears above tile after 2s hover
- [ ] Tooltip has correct styling (dark bg, white text)

### Interaction Testing
- [ ] Move mouse away before 2s → no tooltip
- [ ] Tooltip disappears when mouse leaves tile
- [ ] Tooltip shows correct status for each prerequisite

---

## Definition of Done

- [ ] All feature checklist items completed
- [ ] All unit tests passing
- [ ] All E2E tests passing
- [ ] Lint/TypeScript clean
- [ ] Manual QA completed
- [ ] Git tag created: `v0.4.32b`

---

## Release Notes Draft

```markdown
## v0.4.32b – Scheduler Tile Blocking Visual & Tooltip

**Release Date:** TBD

### Summary

Visual feedback on scheduler tiles now indicates when elements are blocked due to missing prerequisites.

### What's New

**Blocking Indicator:**
- Blocked tiles show dashed left border (vs solid for ready)
- At a glance, see which elements cannot start yet

**Prerequisite Tooltip:**
- Hover over a blocked tile for 2 seconds to see details
- Shows status of Paper, BAT, and Plates
- ⚠ indicates blocking items, ✓ indicates ready items

### Visual Guide

| Border Style | Meaning |
|--------------|---------|
| ━━━━ (solid) | Ready for production |
| ┄┄┄┄ (dashed) | Blocked - check prerequisites |
```

---

## Post-Release

- [ ] Verify blocked tiles show dashed border
- [ ] Verify tooltip appears after 2s hover
- [ ] Update roadmap status
