# Release v0.4.17 – JCF: Papier Autocomplete

> **Status:** ✅ Released
>
> **Milestone:** M4 (JCF)
>
> **Release Date:** 2026-01-28
>
> **Git Tag:** `v0.4.17`

---

## Overview

Replaces the plain text input for the "Papier" field in the JCF Elements Table with a two-step autocomplete component. Users first select a paper type (e.g., "Couché mat"), then a grammage (e.g., "135"). The stored DSL format is `Type:Grammage` (e.g., `Couché mat:135`), while the pretty display shows `Type Grammageg` (e.g., `Couché mat 135g`).

---

## Prerequisites

- [x] `v0.4.16` released (Quantité & Pagination Inputs)
- [x] `PaperType` interface exists in `@flux/types` (`{ id, type, grammages }`)
- [x] `PAPER_TYPES` reference data exists in `mock/reference-data.ts`
- [x] `useSessionLearning` hook already supports `learnPapier` (compound merge)
- [x] `JcfAutocomplete` base component exists

---

## Spec Source

- **§1.1** — Papier Format (from `reference/jcf/docs/implicit-logic-specification.md`)

## Reference Files (read-only)

- `reference/jcf/src/components/PapierAutocomplete.tsx` — behavioral reference

---

## Scope

### In Scope

- Two-step autocomplete: type selection, then grammage selection
- DSL format `Type:Grammage` (colon separator)
- Pretty display `Type Grammageg` when unfocused
- Bidirectional DSL <-> pretty conversion
- Session learning: auto-learn new paper type or grammage on blur
- Grammage suggestions filtered by selected type
- Type suggestions filtered by input
- Integration into JcfElementsTable (replace plain input)
- Table navigation delegation (Tab/Shift+Tab, Alt+Arrow)
- Unit tests (DSL utilities + component)
- E2E tests (two-step selection, keyboard navigation)

### Out of Scope

- Server-side paper type persistence (session-only learning)
- Paper type management UI (CRUD)
- Integration with other autocomplete fields (e.g., cross-field validation)

---

## Related Documentation

### Domain Model

| Document | Sections |
|----------|----------|
| [Domain Vocabulary](../domain-model/domain-vocabulary.md) | Paper, Grammage |
| [Business Rules](../domain-model/business-rules.md) | BR-PAPER-001 to BR-PAPER-003 |

### Spec

| Document | Sections |
|----------|----------|
| implicit-logic-specification.md | §1.1 (Papier Format) |

---

## Technical Notes

- **Two-step autocomplete**: Unlike single-step autocompletes (Impression, Surfacage), Papier requires a two-step flow. When the user selects a type, `:` is appended and suggestions switch to grammages for that type.
- **DSL parsing**: The colon (`:`) is the separator. `lastIndexOf(':')` is used to handle type names that might contain special characters.
- **Grammage normalization**: Grammages are stored as numbers in `PaperType.grammages[]` but displayed with `g` suffix in pretty format.
- **Session learning compound merge**: `learnPapier` uses replace-merge strategy — if the type exists, the full entry is replaced (allowing grammage list extension). The component must build the merged grammage list before calling `learnPapier`.
- **Component wraps JcfAutocomplete**: Unlike the reference implementation which is standalone, the main app implementation wraps `JcfAutocomplete` for consistency. The component manages its own suggestion list that switches between types and grammages based on input state.

---

## Feature Checklist

### DSL Utilities (`papierDsl.ts`)

- [x] **`toPrettyPapier(dsl)` — DSL to pretty display**
  - Description: `"Couché mat:135"` -> `"Couché mat 135g"`
  - Files: `apps/web/src/components/JcfPapierAutocomplete/papierDsl.ts`
  - Tests: `apps/web/src/components/JcfPapierAutocomplete/papierDsl.test.ts`

- [x] **`toDslPapier(pretty)` — Pretty to DSL**
  - Description: `"Couché mat 135g"` -> `"Couché mat:135"`
  - Files: `apps/web/src/components/JcfPapierAutocomplete/papierDsl.ts`
  - Tests: `apps/web/src/components/JcfPapierAutocomplete/papierDsl.test.ts`

- [x] **`isValidPapier(value)` — Validation**
  - Description: Must contain `:` separator with non-empty type and numeric grammage
  - Files: `apps/web/src/components/JcfPapierAutocomplete/papierDsl.ts`
  - Tests: `apps/web/src/components/JcfPapierAutocomplete/papierDsl.test.ts`

- [x] **`parsePapierInput(input)` — Input parser**
  - Description: Parse editing input into `{ type, grammage, isTypingGrammage }` based on `:` position
  - Files: `apps/web/src/components/JcfPapierAutocomplete/papierDsl.ts`
  - Tests: `apps/web/src/components/JcfPapierAutocomplete/papierDsl.test.ts`

### Component (`JcfPapierAutocomplete.tsx`)

- [x] **Two-step autocomplete: type -> grammage**
  - Description: When typing without `:`, show type suggestions. After selecting a type (or typing `:`), show grammage suggestions for that type.
  - Files: `apps/web/src/components/JcfPapierAutocomplete/JcfPapierAutocomplete.tsx`
  - Tests: `apps/web/src/components/JcfPapierAutocomplete/JcfPapierAutocomplete.test.tsx`

- [x] **Pretty display when unfocused**
  - Description: Show `"Couché mat 135g"` when unfocused, `"Couché mat:135"` when focused
  - Files: `apps/web/src/components/JcfPapierAutocomplete/JcfPapierAutocomplete.tsx`
  - Tests: `apps/web/src/components/JcfPapierAutocomplete/JcfPapierAutocomplete.test.tsx`

- [x] **Session learning on blur**
  - Description: Learn new paper type or new grammage for existing type on blur
  - Files: `apps/web/src/components/JcfPapierAutocomplete/JcfPapierAutocomplete.tsx`
  - Tests: `apps/web/src/components/JcfPapierAutocomplete/JcfPapierAutocomplete.test.tsx`

- [x] **Barrel export**
  - Files: `apps/web/src/components/JcfPapierAutocomplete/index.ts`

### Integration

- [x] **Replace plain input in ElementsTable**
  - Description: Replace `<input>` for `papier` row with `<JcfPapierAutocomplete>`
  - Files: `apps/web/src/components/JcfElementsTable/JcfElementsTable.tsx`

- [x] **Import PAPER_TYPES reference data**
  - Files: `apps/web/src/components/JcfElementsTable/JcfElementsTable.tsx`

- [x] **Wire session learning (learnPapier)**
  - Files: `apps/web/src/components/JcfElementsTable/JcfElementsTable.tsx`

### E2E Tests

- [x] **Two-step selection flow**
  - Description: Type -> select type -> grammage suggestions appear -> select grammage
  - Files: `apps/web/playwright/jcf-papier-autocomplete.spec.ts`

- [x] **Keyboard navigation**
  - Description: Arrow keys, Enter, Escape, Tab work correctly in both steps
  - Files: `apps/web/playwright/jcf-papier-autocomplete.spec.ts`

- [x] **Pretty display verification**
  - Description: Verify pretty format shown after selection and blur
  - Files: `apps/web/playwright/jcf-papier-autocomplete.spec.ts`

---

## UX Fidelity Checklist

- [x] Two-step flow: type selection keeps dropdown open for grammage
- [x] Type suggestions filtered by typed input
- [x] Grammage suggestions filtered for selected type
- [x] Pretty display `"Type Grammageg"` when unfocused
- [x] DSL display `"Type:Grammage"` when focused
- [x] Keyboard navigation (Up/Down/Enter/Escape) works in both steps
- [x] Tab/Shift+Tab delegates to table navigation
- [x] Alt+Arrow delegates to table navigation
- [x] Highlight match text in suggestions
- [x] Category badges not used (grammage is the label itself)

---

## Manual QA Test Plan

### Test Fixture
- **Fixture name:** `papier-autocomplete`
- **URL:** `http://localhost:5173/?fixture=papier-autocomplete`
- **Description:** Pre-populated elements with various paper values for testing

### Visual Verification
- [x] Papier cell renders as autocomplete (not plain input)
- [x] Pretty format displayed when unfocused (e.g., "Couché mat 135g")
- [x] DSL format displayed when focused (e.g., "Couché mat:135")
- [x] Dropdown appears on focus with type suggestions
- [x] Dropdown styling matches other autocomplete fields (Format, Impression)

### Interaction Testing
- [x] Click on Papier cell opens autocomplete with type list
- [x] Typing filters type suggestions (e.g., "cou" shows "Couché mat", "Couché satin", "Couché brillant")
- [x] Selecting a type appends ":" and shows grammage suggestions
- [x] Typing ":" manually after type name switches to grammage suggestions
- [x] Selecting a grammage closes dropdown and stores DSL value
- [x] Keyboard: Arrow Down/Up navigates suggestions
- [x] Keyboard: Enter selects highlighted suggestion
- [x] Keyboard: Escape closes dropdown
- [x] Tab moves to next cell (Impression)
- [x] Shift+Tab moves to previous cell (Format)
- [x] Alt+Arrow navigates between elements

### Session Learning
- [x] Typing a new type:grammage and blurring adds it to suggestions
- [x] Re-opening shows the learned entry
- [x] Typing an existing type with new grammage extends its grammage list

### Cross-check with reference/jcf
- [x] Open reference/jcf side-by-side — two-step behavior matches
- [x] Keyboard navigation speed and feel matches
- [x] Visual spacing and sizing proportionally consistent

---

## Testing Requirements

### Unit Tests

- [x] `papierDsl.ts` — `toPrettyPapier`, `toDslPapier`, `isValidPapier`, `parsePapierInput`
- [x] `JcfPapierAutocomplete.tsx` — rendering, focus/blur, suggestions, session learning, navigation

### E2E Tests

- [x] Two-step type/grammage selection
- [x] Keyboard navigation in both steps
- [x] Pretty display after selection

### Manual Testing

- [x] Side-by-side comparison with reference/jcf
- [x] All Manual QA Test Plan items verified

---

## Definition of Done

- [x] All feature checklist items completed
- [x] All unit tests passing
- [x] All E2E tests passing
- [x] Manual testing completed
- [x] TypeScript strict mode — no errors
- [x] ESLint — no warnings
- [x] UX fidelity checklist — all checked
- [x] No code copied from reference/jcf/
- [x] Git tag created: `v0.4.17`
- [x] Release roadmap updated

---

## Release Notes Draft

```markdown
## v0.4.17 – JCF: Papier Autocomplete

**Release Date:** 2026-01-28

### Summary

Adds a two-step autocomplete for the Papier field in the JCF Elements Table. Users select a paper type first, then a grammage. Values are stored in DSL format (`Type:Grammage`) and displayed in pretty format (`Type Grammageg`) when the field is not focused.

### What's New

- Two-step Papier autocomplete (type -> grammage)
- DSL format: `Couché mat:135`
- Pretty display: `Couché mat 135g`
- Session learning for new paper types and grammages
- Full keyboard navigation support
- Table navigation delegation (Tab, Alt+Arrow)

### Breaking Changes

- None
```

---

## Post-Release

- [x] GitHub release created
- [x] Release roadmap updated
- [x] Feature branch merged to `feature/jcf`
