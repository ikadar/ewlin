# Release v0.5.16 – Production Flow Dashboard — Filtering & Navigation

> **Status:** ✅ Released (on ux-ui-development)
>
> **Milestone:** M5 — Phase 5E
>
> **Release Date:** 2026-03-02
>
> **Git Tag:** `v0.5.16`

---

## Overview

A második lépés a Production Flow Dashboard megvalósításában: interaktív szűrés, URL-alapú tab navigáció és billentyűzetes vezérlés. A v0.5.15-ben felépített read-only táblázatra rákerül a keresősáv, az 5-tabos szűrő, és a teljes keyboard shortcut rendszer. A tab állapota nested route-ban él (`/flux`, `/flux/prepresse`, stb.), így böngészőfrissítés után megmarad.

---

## Prerequisites

- [x] `v0.5.15` released — Production Flow Dashboard static table

---

## Scope

### In Scope

- Nested routes: `/flux` (Tous), `/flux/prepresse`, `/flux/papier`, `/flux/formes`, `/flux/plaques`
- Tab bar: 5 tab aktív/inaktív vizuális állapottal, dinamikus count badge-ekkel
- Count badge-ek: tab szűrő + aktív keresés kombinációja alapján számolódik
- Full-text keresés: real-time, case-insensitive, összes szöveges oszlopra (ID, Client, Designation, Transporteur, prerequisite badge label)
- Toolbar: "Flux de production" cím, "Nouveau job" gomb (`Alt+N` hint badge), keresősáv
- Keyboard shortcuts: `Alt+←/→` (tab váltás), `Alt+↑/↓` (sor navigáció), `Alt+F` (keresőmező fókusz), `Alt+N` (új job → `/job/new`)
- Keyboard hint bar a tab bar mellett
- Filterezés multi-element sorokra az aggregált worst értékek alapján (a parent sor aggregált badge-ét vizsgálja, nem az egyes elemekét)

### Out of Scope

- Prerequisite listbox dropdown-ok (v0.5.17)
- Multi-element expand/collapse animáció (v0.5.17)
- Delete/Edit akciók (v0.5.17)
- Parti toggle interaktivitás (v0.5.17)
- Station oszlopok rendezhetősége (halasztva — qa.md K9.2)
- Alt+↑/↓ valódi DOM focus kezelés (v0.5.17-ben DOM focus a listbox-okhoz szükséges; v0.5.16-ban visual highlight elegendő)
- API integráció (v0.5.18)

---

## Related Documentation

### Specification

| Document | Sections |
|----------|----------|
| [tableau-de-flux.md](../production-flow-dashboard-spec/tableau-de-flux.md) | 3.2 (Toolbar), 3.3 (Tab Bar), 3.4 (Keyboard Shortcuts), 3.5 (URL Persistence), 6.4 (Filter Functions), 6.5 (Filter Verification), 6.6 (Sub-row Association), 6.7 (Edge Cases) |
| [qa.md](../production-flow-dashboard-spec/qa.md) | K4.1 (count + search), K11.1 (nested routes) |

### Domain Model

| Document | Sections |
|----------|----------|
| [Domain Vocabulary](../domain-model/domain-vocabulary.md) | Job, Element, ElementPrerequisite |
| [Business Rules](../domain-model/business-rules.md) | BR-PREREQ-001–007 |

---

## Technical Notes

- **Nested routes:** A `routes.tsx`-ben a `/flux` route-hoz nested route-ok kerülnek (index + 4 child). A `FluxPage` a `useLocation`-ból olvassa az aktív tab-ot.
- **Tab ↔ route mapping:** `'/flux' → 'all'`, `'/flux/prepresse' → 'prepresse'`, stb. Navigáció `useNavigate()`-tel.
- **Search state:** `FluxPage`-en belül `useState`, nem URL-ben (spec szerint: tab az URL-ben, keresés nem).
- **Count badge számítás:** Minden tab countja = a jobList szűrve (tab filter AND search) darabszáma. A `counts` objektum minden tab-hoz tartalmaz egy értéket, mindig az összes ötre számolva.
- **Filtering:** `FluxPage` szűri a `jobs` tömböt tab + search alapján, és a szűrt tömböt adja át a `FluxTable`-nek. A `FluxTable` belső rendezése (ID asc) a szűrt tömbön működik.
- **Search scope:** A parent sor szöveges mezői: `id`, `client`, `designation`, `transporteur`. Prerequisite badge label-ek: a parent soron megjelenített (worst) értékek — `bat`, `papier`, `formes`, `plaques` stringek. Sub-row szövegek NEM kerülnek keresésre (a spec sub-row visibility-t a parent filter-eredményéhez köti).
- **Keyboard shortcuts:** `useEffect` + `document.addEventListener('keydown')`. `Alt+F`: `searchRef.current?.focus()`. `Alt+N`: `navigate('/job/new')`. `Alt+←/→`: tab ciklus. `Alt+↑/↓`: `focusedRowIndex` state, scroll into view.
- **Alt+↑/↓ implementáció:** A FluxPage tartja a `focusedRowIndex`-et (visible parent rows indexébe), és egy `data-flux-row-focused` attribútumot set-el a megfelelő sorra. CSS kiemeli a fókuszált sort (indigo háttér + outline). v0.5.17-ben ez a DOM focus-ra lesz kötve a listbox interakciókhoz.

---

## Feature Checklist

### Routing

- [x] **Nested routes hozzáadása**
  - Description: A `routes.tsx`-ben a `/flux` route-hoz 5 route: `index` (Tous), `prepresse`, `papier`, `formes`, `plaques`. Mindegyik a `FluxPage` komponenst rendereli. A `FluxPage` a pathname-ből olvassa az aktív tab-ot.
  - Files: `src/routes.tsx`, `src/pages/FluxPage.tsx`
  - Tests: E2E: `flux-navigation.spec.ts`

### Toolbar

- [x] **FluxToolbar komponens**
  - Description: "Flux de production" cím (h1, bal), "Nouveau job" gomb (jobb, primary blue) `Alt+N` `<kbd>` badge-gel. Alatta keresősáv: full-width input, `🔍` ikon bal oldalon, placeholder "Rechercher...", `data-testid="flux-search"`. Props: `searchValue`, `onSearchChange`, `onNewJob`.
  - Files: `src/components/FluxToolbar/FluxToolbar.tsx` (új), `src/components/FluxToolbar/index.ts` (új)
  - Tests: `src/components/FluxToolbar/FluxToolbar.test.tsx` (új)

### Tab Bar

- [x] **FluxTabBar komponens**
  - Description: 5 tab (Tous, A faire prepresse, Cdes papier, Cdes formes, Plaques a produire) mindig megjelenik. Aktív tab: kék alul border (2px), magas kontrasztú szöveg, emelt háttér. Inaktív: szürke szöveg, átlátszó háttér, hover = fehér szöveg. Count badge zárójelben a label után (pl. "Tous (5)"). Props: `activeTab`, `counts`, `onTabChange`. `data-testid="flux-tab-{id}"` attribútumok a tab gombokra.
  - Files: `src/components/FluxTabBar/FluxTabBar.tsx` (új), `src/components/FluxTabBar/index.ts` (új)
  - Tests: `src/components/FluxTabBar/FluxTabBar.test.tsx` (új)

- [x] **Keyboard hint bar**
  - Description: A tab bar jobb oldalán `<kbd>` badge-ek a shortcut tájékoztatókhoz: `Alt+←` / `Alt+→` (tab switch), `Alt+↑` / `Alt+↓` (sor), `Alt+F` (keresés), `Alt+N` (új). Vertical bar elválasztókkal. JetBrains Mono betűtípus.
  - Files: `src/components/FluxTabBar/FluxTabBar.tsx`
  - Tests: —

### Filtering Logic

- [x] **fluxFilters.ts — szűrő függvények**
  - Description: `filterByTab(job, tab)` — spec 6.4 filter functions. `filterBySearch(job, search)` — case-insensitive substring keresés: id, client, designation, transporteur, + bat/papier/formes/plaques string értékek. `computeTabCounts(jobs, search)` — returns `Record<TabId, number>`. `TabId` = `'all' | 'prepresse' | 'papier' | 'formes' | 'plaques'`. A filterByTab multi-element soroknál az aggregált worst értékeket használja (a `FluxJob` parent row mezői már az aggregált worst értékeket tartalmazzák — ez a v0.5.15-ös `fluxStaticData.ts`-ben is így van).
  - Files: `src/components/FluxTable/fluxFilters.ts` (új)
  - Tests: `src/components/FluxTable/fluxFilters.test.ts` (új) — spec 6.5 verification matrix összes kombinációja, search edge case-ek

### FluxPage Integration

- [x] **FluxPage újraszabása**
  - Description: Integrálja a FluxToolbar-t és FluxTabBar-t. `useLocation` alapján meghatározza az aktív tab-ot. `useState` a keresőmező értékéhez. `useMemo` a filtered jobs + tab counts számításához. A szűrt jobs-t adja át a FluxTable-nek. Layout: `flex-col`, toolbar (border-b), tab bar (border-b), table (flex-1 overflow).
  - Files: `src/pages/FluxPage.tsx`
  - Tests: `src/pages/FluxPage.test.tsx` (módosítás)

### Keyboard Shortcuts

- [x] **Keyboard shortcut kezelő**
  - Description: `useEffect` + `document.addEventListener('keydown', handler)` a `FluxPage`-ben. `Alt+F`: keresőmező fókuszálása (`searchRef`). `Alt+N`: `navigate('/job/new')`. `Alt+←`: előző tab (ciklikus). `Alt+→`: következő tab (ciklikus). `Alt+↑`: `focusedRowIndex--` (visible rows közt, clamp 0-nál). `Alt+↓`: `focusedRowIndex++` (visible rows közt). Minden shortcut: `e.preventDefault()` az alapértelmezett böngésző viselkedés megakadályozásához.
  - Files: `src/pages/FluxPage.tsx`
  - Tests: E2E: `flux-navigation.spec.ts`

### Row Focus (Alt+↑/↓)

- [x] **Fókuszált sor vizuális kiemelése**
  - Description: A `FluxTable` props-ot kap: `focusedJobId?: string`. Ha meg van adva, a megfelelő `<tr>` kap egy `data-flux-focused="true"` attribútumot és CSS kiemelést (halvány indigo háttér + outline). A FluxPage kezeli a `focusedJobId` state-et (visible sorted rows alapján indexeli).
  - Files: `src/components/FluxTable/FluxTable.tsx`, `src/pages/FluxPage.tsx`
  - Tests: E2E: `flux-navigation.spec.ts`

---

## Testing Requirements

### Unit Tests

- [x] `FluxToolbar` — renderelés, keresés onChange callback, "Nouveau job" gomb onClick
- [x] `FluxTabBar` — mind az 5 tab renderelése, aktív tab kiemelés, count badge-ek, tab kattintás callback
- [x] `fluxFilters` — mind a 6.5 filter verification matrix eset (Tous=5, prepresse=3, papier=2, formes=1, plaques=2), search kombinációk, multi-element filter az aggregált értékek alapján
- [x] `FluxPage` — aktív tab a pathname alapján, keresés szűri a sorokat, count badge-ek a szűrt eredményt tükrözik

### E2E Tests

- [x] Tab click → URL változás (`/flux`, `/flux/prepresse`, stb.)
- [x] Tab click → helyes sorok jelennek meg (spec 6.5 verification matrix)
- [x] Search → sorok valós időben szűrődnek
- [x] Search + tab kombináció → mindkét feltétel teljesülnie kell
- [x] Count badge-ek → helyes számok (Tous=5, prepresse=3, papier=2, formes=1, plaques=2)
- [x] Keyboard: `Alt+→` → következő tab aktív, URL változik
- [x] Keyboard: `Alt+←` → előző tab aktív (wrap: plaques→all)
- [x] Keyboard: `Alt+F` → search input fókuszt kap
- [x] Keyboard: `Alt+N` → `/job/new`-ra navigál
- [x] Oldal reload `/flux/papier` URL-en → tab aktív marad

### Manual Testing

- Lásd Manual QA Test Plan lent.

---

## Manual QA Test Plan

### Test Fixture

- **Fixture neve:** (nincs külön fixture — a v0.5.15 `fluxStaticData.ts` statikus adatai)
- **URL:** `http://localhost:5173/flux`
- **Leírás:** Az 5 referencia job (00042, 00078, 00091, 00103, 00117) a v0.5.15-ből ismerős adatokkal. 00078 és 00091 multi-element sorok.

### Visual Verification

- [x] Toolbar: "Flux de production" cím bal oldalt, "Nouveau job" gomb jobb oldalt Alt+N hint badge-gel
- [x] Keresősáv: full-width, keresőikon bal oldalt, placeholder "Rechercher..."
- [x] Tab bar: 5 tab sorban (Tous, A faire prepresse, Cdes papier, Cdes formes, Plaques a produire)
- [x] Aktív tab: kék alul border, fehér szöveg, emelt háttér
- [x] Inaktív tab hover: szöveg fehérre vált
- [x] Count badge-ek: Tous (5), A faire prepresse (3), Cdes papier (2), Cdes formes (1), Plaques a produire (2)
- [x] Keyboard hint bar: `Alt+←` / `Alt+→` / `Alt+↑` / `Alt+↓` / `Alt+F` / `Alt+N` `<kbd>` badge-ek
- [x] "Tous" tab alapértelmezetten aktív, mind az 5 sor látható

### Interaction Testing

- [x] "A faire prepresse" tab kattintás → URL: `/flux/prepresse`, 3 sor látható (00078, 00091, 00103)
- [x] "Cdes papier" tab kattintás → URL: `/flux/papier`, 2 sor látható (00078, 00091)
- [x] "Cdes formes" tab kattintás → URL: `/flux/formes`, 1 sor látható (00078)
- [x] "Plaques a produire" tab kattintás → URL: `/flux/plaques`, 2 sor látható (00078, 00091)
- [x] "Tous" visszakattintás → URL: `/flux`, 5 sor látható
- [x] Keresés "Ducros" → 1 sor látható (00042), count badge-ek frissülnek (Tous=1, prepresse=0, papier=0, formes=0, plaques=0)
- [x] Keresés "att.fich" (lowercase) → 2 sor látható (00078, 00091) — case-insensitive badge label keresés
- [x] Keresés "Muller" majd "A faire prepresse" tab → 1 sor (00078)
- [x] Keresés törlése → sorok visszaállnak a tab szűrő szerint
- [x] `Alt+→` (Tous-ból) → "A faire prepresse" aktív, URL: `/flux/prepresse`
- [x] `Alt+←` (Tous-ban) → "Plaques a produire" aktív (wrap-around), URL: `/flux/plaques`
- [x] `Alt+F` → keresőmező fókuszt kap (cursor villog)
- [x] `Alt+N` → `/job/new`-ra navigál
- [x] `/flux/papier` URL-re navigálás → "Cdes papier" tab aktív, 2 sor látható
- [x] Oldal reload `/flux/papier` URL-en → "Cdes papier" tab aktív marad, 2 sor látható

### Regression Testing

- [x] A meglévő Scheduling view (`/`) és Settings oldalak (`/settings/*`) változatlanul működnek
- [x] A Sidebar LayoutGrid gomb a `/flux` oldalra navigál
- [x] A táblázat frozen oszlopai és vizuális elemei (badge-ek, ring+dot, stacked dots) változatlanul jelennek meg

---

## Definition of Done

- [x] Minden feature checklist item kész
- [x] Minden unit teszt zöld
- [x] Minden E2E teszt zöld
- [x] Lint és TypeScript typecheck hibamentes
- [x] Manual QA Test Plan minden pontja ✅
- [x] Kód felülvizsgálva
- [x] Git tag: `v0.5.16`
- [x] GitHub release létrehozva (`ux-ui-development` branch)
- [x] Roadmap frissítve

---

## Release Notes Draft

```markdown
## v0.5.16 – Production Flow Dashboard — Filtering & Navigation

**Release Date:** 2026-03-02

### Summary

A Production Flow Dashboard interaktív szűrési és navigációs rétege. A v0.5.15 statikus táblázatára rákerül az 5-tabos szűrő, a full-text kereső, és a teljes keyboard shortcut rendszer. A tab állapota URL-ben él (nested routes), így böngészőfrissítés után megmarad.

### What's New

- Tab bar: 5 szűrő tab (Tous, A faire prepresse, Cdes papier, Cdes formes, Plaques a produire) dinamikus count badge-ekkel
- Nested routes: `/flux`, `/flux/prepresse`, `/flux/papier`, `/flux/formes`, `/flux/plaques`
- Full-text keresés: real-time, case-insensitive, ID + Client + Designation + Transporteur + badge label mezőkre
- Keyboard shortcuts: `Alt+←/→` (tab), `Alt+↑/↓` (sor), `Alt+F` (keresés), `Alt+N` (új job)
- Keyboard hint bar a tab bar mellett
- Toolbar: "Flux de production" cím + "Nouveau job" gomb + keresősáv
```

---

## Post-Release

- [x] GitHub release létrehozva
- [x] Roadmap `v0.5.16` tételei ✅-re frissítve
