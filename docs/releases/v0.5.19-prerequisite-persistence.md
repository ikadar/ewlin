# Release v0.5.19 – Production Flow Dashboard — Prerequisite Persistence

> **Status:** 🟡 In Progress
>
> **Milestone:** M5 — Phase 5E
>
> **Target Date:** 2026-03-02
>
> **Git Tag:** `v0.5.19`

---

## Overview

A v0.5.18 (API Integration) csak az in-memory RTK Query cache-t frissítette prerequisite változtatáskor — az oldalfrissítés elveszítette a változtatásokat. Ez a release hozzáadja a perzisztálást: a listbox-ban végzett módosítás egy `PATCH /api/v1/flux/elements/{id}` hívást indít, és az adatbázisba ment.

RTK Query `onQueryStarted` optimista update pattern: a cache azonnal frissül, és hibás API hívás esetén automatikusan visszaáll (undo).

---

## Prerequisites

- [x] `v0.5.18` released — Production Flow Dashboard API Integration
- [x] `v0.5.18` refactor — PrerequisiteStatus API enum migration

---

## Scope

### PHP API (`services/php-api`)

- **`PATCH /api/v1/flux/elements/{id}`** — egyetlen prerequisite mező frissítése
  - Request body: `{ "column": "bat"|"papier"|"formes"|"plaques", "value": "<enum string>" }`
  - Response: 200 + `FluxElementResponse` JSON, vagy 404/422 hiba esetén
  - Enum mapping: `BatStatus::tryFrom($value)`, `PaperStatus::tryFrom()`, stb.
  - `FluxController::updateElementPrerequisite()` action
  - Element GUID alapján keresi az entity-t (`ElementRepository`)
- **Unit teszt**: `FluxUpdateElementPrerequisiteTest.php`

### Frontend (`apps/web`)

- **`fluxApi.ts`** — `updateElementPrerequisite` RTK Query mutation
  - `PATCH /flux/elements/{elementId}` a mock/real base query-n keresztül
  - `onQueryStarted`: optimistic cache update + undo on error
- **`mockBaseQuery.ts`** — `PATCH /flux/elements/{id}` mock handler
  - In-memory `FLUX_STATIC_JOBS` frissítése (tesztelhetőség)
- **`FluxPage.tsx`** — `handleUpdatePrerequisite` mutation-t hív a dispatch helyett
- **Unit tesztek**: `fluxApi` slice tesztek a mutationra

### Out of Scope

- K5.1 — Parti toggle persistencia (`PUT /api/v1/jobs/{id}/shipped`)
- Error toast UI (konzol log elég most)
- Optimistic update conflict resolution (concurrent edits)

---

## Technical Notes

### RTK Query optimistic update pattern

```ts
updateElementPrerequisite: builder.mutation<void, UpdatePrerequisiteArg>({
  query: ({ elementId, column, value }) => ({
    url: `/flux/elements/${elementId}`,
    method: 'PATCH',
    body: { column, value },
  }),
  async onQueryStarted({ elementId, column, value }, { dispatch, queryFulfilled }) {
    const patchResult = dispatch(
      fluxApi.util.updateQueryData('getFluxJobs', undefined, (draft) => {
        for (const job of draft) {
          const el = job.elements.find(e => e.id === elementId);
          if (el) { el[column] = value; break; }
        }
      }),
    );
    try {
      await queryFulfilled;
    } catch {
      patchResult.undo();
    }
  },
}),
```

### Mock mode

A `mockBaseQuery.ts` `PATCH /flux/elements/{id}` kérésekre `{ data: {} }` választ ad — az optimistic update már megtörtént az `onQueryStarted`-ben, a mock response nincs felhasználva.

### Element ID

A `FluxElement.id` mező: mock módban `'00042-e1'` stílusú, valós API módban PHP GUID. A frontend mindkét esetben ugyanúgy kezeli. A PATCH URL-ben az `elementId`-t az `element.id` mezőből vesszük.

---

## Feature Checklist

### PHP API

- [ ] **`FluxController::updateElementPrerequisite()`** — `PATCH /api/v1/flux/elements/{id}`
  - Files: `services/php-api/src/Controller/FluxController.php`
  - Tests: `tests/Unit/Controller/FluxUpdateElementPrerequisiteTest.php`

- [ ] **Column → Setter mapping** a controller action-ben
  - `bat` → `setBatStatus(BatStatus::tryFrom($value))`
  - `papier` → `setPaperStatus(PaperStatus::tryFrom($value))`
  - `formes` → `setFormeStatus(FormeStatus::tryFrom($value))`
  - `plaques` → `setPlateStatus(PlateStatus::tryFrom($value))`

### Frontend

- [ ] **`updateElementPrerequisite` mutation** — `fluxApi.ts`
  - Files: `apps/web/src/store/api/fluxApi.ts`

- [ ] **`PATCH /flux/elements/{id}` mock handler** — `mockBaseQuery.ts`
  - Files: `apps/web/src/store/api/mockBaseQuery.ts`

- [ ] **`FluxPage.handleUpdatePrerequisite`** — mutation hívás
  - Files: `apps/web/src/pages/FluxPage.tsx`

- [ ] **Unit tesztek** — mutation + mock handler
  - Files: `apps/web/src/store/api/fluxApi.test.ts` (ha létezik, egyébként új)

---

## Manual QA Test Plan

### Test Fixture
- **Fixture name:** `prerequisite-save`
- **URL:** `http://localhost:5173/?fixture=prerequisite-save`
- **Description:** Standard 5 job fixture, mock mode-ban PATCH handler aktív

### Interaction Testing
- [ ] Egy BAT értéket megváltoztat → UI azonnal frissül (optimistic)
- [ ] Oldalfrissítés után az érték visszaáll → mock módban igen (in-memory), valós API módban megmarad
- [ ] 4 különböző oszlop (BAT, Papier, Formes, Plaques) mind menthetők
- [ ] Sub-sorban végzett változtatás → szülősor aggregált értéke frissül
- [ ] Tab count badge-ek újraszámolódnak változtatás után

### Regression Testing
- [ ] Dropdown megjelenése és bezárása változatlan
- [ ] Keresés és tab szűrés működik
- [ ] Expand/collapse sub-sorokkal együtt

---

## Definition of Done

- [ ] PHP unit tesztek zöld
- [ ] Frontend unit tesztek zöld (2149+)
- [ ] Lint + TypeScript hibamentes
- [ ] Mock módban az optimistic update működik
- [ ] Valós API módban az érték az adatbázisba mentődik
- [ ] Git tag: `v0.5.19`
- [ ] Roadmap frissítve
