# Release v0.4.18 -- JCF: Imposition Autocomplete

> **Status:** Not Started
>
> **Milestone:** M4 (JCF)
>
> **Target Date:** 2026-01-28
>
> **Git Tag:** `v0.4.18`

---

## Overview

Replaces the plain text input for the "Imposition" field in the JCF Elements Table with a two-step autocomplete component. Users first select a sheet format (e.g., "50x70"), then a poses count (e.g., "8"). The stored DSL format is `LxH(poses)` (e.g., `50x70(8)`), while the pretty display shows `LxHcm Nposes/f` (e.g., `50x70cm 8poses/f`).

---

## Prerequisites

- [x] `v0.4.17` released (Papier Autocomplete)
- [x] `FeuilleFormat` interface exists in `@flux/types` (`{ format: string; poses: number[] }`)
- [x] `FEUILLE_FORMATS` reference data exists in `mock/reference-data.ts`
- [x] `useSessionLearning` hook already supports `learnFeuilleFormat` (poses-merge)
- [x] `JcfAutocomplete` base component exists

---

## Spec Source

- **Section 1.2** -- Imposition format (from `reference/jcf/docs/implicit-logic-specification.md`)

## Reference Files (read-only)

- `reference/jcf/src/components/ImpositionAutocomplete.tsx` -- behavioral reference
- `reference/jcf/src/data/impositions.ts` -- validation and parsing utilities

---

## Scope

### In Scope

- Two-step autocomplete: sheet format selection, then poses selection
- DSL format `LxH(poses)` (parenthesis separator)
- Pretty display conversion: `50x70(8)` -> `50x70cm 8poses/f`
- Bidirectional DSL <-> pretty conversion
- Validation regex: `/^[1-9]\d*x[1-9]\d*\([1-9]\d*\)$/i`
- Poses extraction supporting `50x70(8)` and `50x70(8p)` variants
- Session learning: auto-learn new format or poses on blur
- Format suggestions filtered by input
- Poses suggestions filtered by selected format
- Integration into JcfElementsTable (replace plain input)
- Table navigation delegation (Tab/Shift+Tab, Alt+Arrow)
- Unit tests (DSL utilities + component)
- E2E tests (two-step selection, keyboard navigation)

### Out of Scope

- Format-aware suggestions based on the product format field (cross-field logic -- deferred)
- Server-side format persistence (session-only learning)

---

## Related Documentation

### Spec

| Document | Sections |
|----------|----------|
| implicit-logic-specification.md | Section 1.2 (Imposition format) |

---

## Technical Notes

- **Two-step autocomplete**: Similar to Papier (v0.4.17). When the user selects a sheet format, `(` is appended and suggestions switch to poses for that format. When poses is selected, `)` is appended to complete the DSL.
- **DSL parsing**: The opening parenthesis `(` is the separator. `indexOf('(')` splits format from poses. The closing `)` is stripped during editing and re-added on completion.
- **Poses storage**: `FeuilleFormat.poses` is `number[]` in `@flux/types`. Displayed as strings in suggestions.
- **Session learning**: `learnFeuilleFormat` uses poses-merge strategy -- if the format exists, new poses are merged into the existing poses array. If not, a new entry is added.
- **Standalone component**: Following the JcfPapierAutocomplete pattern -- standalone component reusing shared utilities (`useLazyLoadSuggestions`, `highlightMatch`, `mergeWithSession`).
- **Pretty format**: `50x70cm 8poses/f` -- adds `cm` after dimensions and `poses/f` after count.

---

## Feature Checklist

### DSL Utilities (`impositionDsl.ts`)

- [ ] **`toPrettyImposition(dsl)` -- DSL to pretty display**
  - Description: `"50x70(8)"` -> `"50x70cm 8poses/f"`
  - Files: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.ts`
  - Tests: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.test.ts`

- [ ] **`toDslImposition(pretty)` -- Pretty to DSL**
  - Description: `"50x70cm 8poses/f"` -> `"50x70(8)"`
  - Files: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.ts`
  - Tests: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.test.ts`

- [ ] **`isValidImposition(value)` -- Validation**
  - Description: Regex `/^[1-9]\d*x[1-9]\d*\([1-9]\d*\)$/i` -- non-zero positive integers for width, height, and poses
  - Files: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.ts`
  - Tests: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.test.ts`

- [ ] **`parseImposition(value)` -- Structured parser**
  - Description: Parse `"50x70(8)"` into `{ width: 50, height: 70, poses: 8 }` or null
  - Files: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.ts`
  - Tests: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.test.ts`

- [ ] **`parseImpositionInput(input)` -- Editing input parser**
  - Description: Parse editing input into `{ format, poses, isTypingPoses }` based on `(` position
  - Files: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.ts`
  - Tests: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.test.ts`

- [ ] **`extractPosesFromImposition(value)` -- Poses extraction**
  - Description: Extract poses from DSL or pretty format. Supports `50x70(8)`, `50x70(8p)`, `8 poses` variants
  - Files: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.ts`
  - Tests: `apps/web/src/components/JcfImpositionAutocomplete/impositionDsl.test.ts`

### Component (`JcfImpositionAutocomplete.tsx`)

- [ ] **Two-step autocomplete: format -> poses**
  - Description: When typing without `(`, show format suggestions. After selecting a format (or typing `(`), show poses suggestions for that format.
  - Files: `apps/web/src/components/JcfImpositionAutocomplete/JcfImpositionAutocomplete.tsx`
  - Tests: `apps/web/src/components/JcfImpositionAutocomplete/JcfImpositionAutocomplete.test.tsx`

- [ ] **Pretty display when unfocused**
  - Description: Show `"50x70cm 8poses/f"` when unfocused, `"50x70(8)"` when focused
  - Files: `apps/web/src/components/JcfImpositionAutocomplete/JcfImpositionAutocomplete.tsx`
  - Tests: `apps/web/src/components/JcfImpositionAutocomplete/JcfImpositionAutocomplete.test.tsx`

- [ ] **Session learning on blur**
  - Description: Learn new sheet format or new poses for existing format on blur
  - Files: `apps/web/src/components/JcfImpositionAutocomplete/JcfImpositionAutocomplete.tsx`
  - Tests: `apps/web/src/components/JcfImpositionAutocomplete/JcfImpositionAutocomplete.test.tsx`

- [ ] **Barrel export**
  - Files: `apps/web/src/components/JcfImpositionAutocomplete/index.ts`

### Integration

- [ ] **Replace plain input in ElementsTable**
  - Description: Replace `<input>` for `imposition` row with `<JcfImpositionAutocomplete>`
  - Files: `apps/web/src/components/JcfElementsTable/JcfElementsTable.tsx`

- [ ] **Import FEUILLE_FORMATS reference data**
  - Files: `apps/web/src/components/JcfElementsTable/JcfElementsTable.tsx`

- [ ] **Wire session learning (learnFeuilleFormat)**
  - Files: `apps/web/src/components/JcfElementsTable/JcfElementsTable.tsx`

### E2E Tests

- [ ] **Two-step selection flow**
  - Description: Format -> select format -> poses suggestions appear -> select poses
  - Files: `apps/web/playwright/jcf-imposition-autocomplete.spec.ts`

- [ ] **Keyboard navigation**
  - Description: Arrow keys, Enter, Escape, Tab work correctly in both steps
  - Files: `apps/web/playwright/jcf-imposition-autocomplete.spec.ts`

- [ ] **Pretty display verification**
  - Description: Verify pretty format shown after selection and blur
  - Files: `apps/web/playwright/jcf-imposition-autocomplete.spec.ts`

---

## UX Fidelity Checklist

- [ ] Two-step flow: format selection appends `(` and keeps dropdown open for poses
- [ ] Format suggestions filtered by typed input
- [ ] Poses suggestions filtered for selected format
- [ ] Pretty display `"LxHcm Nposes/f"` when unfocused
- [ ] DSL display `"LxH(N)"` when focused
- [ ] Keyboard navigation (Up/Down/Enter/Escape) works in both steps
- [ ] Tab/Shift+Tab delegates to table navigation
- [ ] Alt+Arrow delegates to table navigation
- [ ] Highlight match text in suggestions
- [ ] Selecting poses appends closing `)` and closes dropdown

---

## Manual QA Test Plan

### Test Fixture
- **Fixture name:** Use existing test fixture (default JCF modal)
- **URL:** `http://localhost:5173/?fixture=test`
- **Description:** Default JCF modal with elements table; imposition is row 8

### Visual Verification
- [ ] Imposition cell renders as autocomplete (not plain input)
- [ ] Pretty format displayed when unfocused (e.g., "50x70cm 8poses/f")
- [ ] DSL format displayed when focused (e.g., "50x70(8)")
- [ ] Dropdown appears on focus with format suggestions
- [ ] Dropdown styling matches other autocomplete fields (Papier, Format, Impression)

### Interaction Testing
- [ ] Click on Imposition cell opens autocomplete with format list
- [ ] Typing filters format suggestions (e.g., "50" shows "50x70")
- [ ] Selecting a format appends "(" and shows poses suggestions
- [ ] Typing "(" manually after format name switches to poses suggestions
- [ ] Selecting poses closes dropdown and stores DSL value with closing ")"
- [ ] Keyboard: Arrow Down/Up navigates suggestions
- [ ] Keyboard: Enter selects highlighted suggestion
- [ ] Keyboard: Escape closes dropdown
- [ ] Tab moves to next cell (Impression)
- [ ] Shift+Tab moves to previous cell (Papier)
- [ ] Alt+Arrow navigates between elements

### Session Learning
- [ ] Typing a new format(poses) and blurring adds it to suggestions
- [ ] Re-opening shows the learned entry
- [ ] Typing an existing format with new poses extends its poses list

### Cross-check with reference/jcf
- [ ] Open reference/jcf side-by-side -- two-step behavior matches
- [ ] Keyboard navigation speed and feel matches
- [ ] Visual spacing and sizing proportionally consistent

---

## Testing Requirements

### Unit Tests

- [ ] `impositionDsl.ts` -- `toPrettyImposition`, `toDslImposition`, `isValidImposition`, `parseImposition`, `parseImpositionInput`, `extractPosesFromImposition`
- [ ] `JcfImpositionAutocomplete.tsx` -- rendering, focus/blur, suggestions, session learning, navigation

### E2E Tests

- [ ] Two-step format/poses selection
- [ ] Keyboard navigation in both steps
- [ ] Pretty display after selection

### Manual Testing

- [ ] Side-by-side comparison with reference/jcf
- [ ] All Manual QA Test Plan items verified

---

## Definition of Done

- [ ] All feature checklist items completed
- [ ] All unit tests passing
- [ ] All E2E tests passing
- [ ] Manual testing completed
- [ ] TypeScript strict mode -- no errors
- [ ] ESLint -- no warnings
- [ ] UX fidelity checklist -- all checked
- [ ] No code copied from reference/jcf/
- [ ] Git tag created: `v0.4.18`
- [ ] Release roadmap updated

---

## Release Notes Draft

```markdown
## v0.4.18 -- JCF: Imposition Autocomplete

**Release Date:** 2026-01-28

### Summary

Adds a two-step autocomplete for the Imposition field in the JCF Elements Table. Users select a sheet format first (e.g., "50x70"), then a poses count (e.g., "8"). Values are stored in DSL format (`LxH(poses)`) and displayed in pretty format (`LxHcm Nposes/f`) when the field is not focused.

### What's New

- Two-step Imposition autocomplete (format -> poses)
- DSL format: `50x70(8)`
- Pretty display: `50x70cm 8poses/f`
- Session learning for new sheet formats and poses
- Full keyboard navigation support
- Table navigation delegation (Tab, Alt+Arrow)

### Breaking Changes

- None
```

---

## Post-Release

- [ ] GitHub release created
- [ ] Release roadmap updated
- [ ] Feature branch merged to `feature/jcf`
