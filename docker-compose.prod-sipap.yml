# Production overrides for SIPAP instance (sipap.replic-os.com)
# Usage: docker compose --env-file .env.production -f docker-compose.yml -f docker-compose.prod-sipap.yml up -d

services:
  nginx:
    profiles:
      - dev-only

  php:
    container_name: sipap-php
    user: "1001:1001"
    ports:
      - "127.0.0.1:9002:9000"
    volumes:
      - ./docker/php/php-prod.ini:/usr/local/etc/php/php.ini:ro
    environment:
      - APP_ENV=prod
      - APP_DEBUG=0
    restart: unless-stopped
    networks:
      flux-network: null
    depends_on:
      mariadb:
        condition: service_healthy

  validation-service:
    container_name: sipap-validation-service
    ports: !reset []
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      flux-network: null

  compaction-service:
    container_name: sipap-compaction-service
    ports: !reset []
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      flux-network: null

  mariadb:
    container_name: sipap-mariadb
    ports: !override
      - "127.0.0.1:3308:3306"
    volumes:
      - sipap-mariadb-data:/var/lib/mysql
    restart: unless-stopped
    networks:
      flux-network: null

  mercure:
    container_name: sipap-mercure
    environment:
      MERCURE_EXTRA_DIRECTIVES: |
        cors_origins ${CORS_ALLOW_ORIGIN}
        anonymous
    ports: !override
      - "127.0.0.1:3003:3000"
    restart: unless-stopped
    networks:
      flux-network: null

  redis:
    profiles:
      - dev-only

  node:
    profiles:
      - dev-only

networks:
  flux-network:
    name: sipap-network
    driver: bridge

volumes:
  sipap-mariadb-data:
    name: sipap-mariadb-data
  flux-mariadb-data:
    name: sipap-mariadb-data
